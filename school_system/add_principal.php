<?php
include 'db_connect.php';

/*
$principalPassword = password_hash("Principal@123", PASSWORD_DEFAULT);

// Insert Principal into Users table
$sql = "INSERT INTO Users (Username, Password, Role) VALUES ('principal', '$principalPassword', 'Principal')";
if ($conn->query($sql) === TRUE) {
    echo "Principal added successfully";
} else {
    echo "Error: " . $conn->error;
}

$conn->close();*/


?>
